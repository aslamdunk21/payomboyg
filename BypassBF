-- ─── ANTI-STUN / ANTI-KNOCKBACK / ANTI-SIT ───
task.spawn(function()
    while scriptRunning and task.wait(0.08) do
        local char = LocalPlayer.Character
        if not char then continue end
        
        -- ลบ status effects ทันทีก่อนที่จะล็อกการเคลื่อนไหว
        for _, effect in ipairs({"Stun", "Busy", "Paralyzed", "Frozen", "Sleeping", "Knockback"}) do
            local obj = char:FindFirstChild(effect)
            if obj then
                if obj:IsA("BoolValue") then obj.Value = false end
                if obj:IsA("IntValue") then obj.Value = 0 end
                obj:Destroy()
            end
        end
        
        -- บังคับ Sit = false (ป้องกันโดนล็อกท่านั่ง)
        local humanoid = char:FindFirstChild("Humanoid")
        if humanoid then
            if humanoid.Sit then humanoid.Sit = false end
            if humanoid.PlatformStand then humanoid.PlatformStand = false end
        end
        
        -- ลบ BodyVelocity ที่เกมอาจ inject มา (เช่น knockback)
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            for _, v in ipairs(hrp:GetChildren()) do
                if v:IsA("BodyVelocity") and v.Name ~= "fixvelo" and v.Name ~= "BringVelocity" then
                    v:Destroy()
                end
            end
        end
    end
end)
